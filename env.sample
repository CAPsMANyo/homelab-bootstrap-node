REDIS_ADDR=           # main node IP:6379, e.g. 10.10.9.1:6379
REDIS_PASSWORD=
NODE_IP=              # this node's LAN IP (used by traefik-kop as backend IP for routes)
NODE_NAME=            # short name for this node, e.g. gpu, truenas (used for hawser-{NODE_NAME}.{DOMAIN})
DOMAIN=
TZ=
